import 'package:flutter/material.dart';

class dis extends StatelessWidget {
  const dis({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(backgroundColor: Colors.pink,));
  }
}
